#!/usr/bin/env python
# -*- coding: utf-8 -*-

''' '''

__author__ = 'Wang Junqi'

import csv;

def mian(file_name, featrue_name):
    csv_reader=csv.reader(open(file_name,'rb'));
    featrue_info = csv_reader.next();
    for featrue in featrue_info:
        d
    if remove_num == 372:
        print 'There are only based features!';
        return;
    csv_writer=csv.writer(open('rm_%s_%s' % (remove_num,file_name) ));
    csv_writer.writerow(nums[:-1]);
    for row in csv_reader:
        csv_writer.writerow(row[:-remove_num]);

if __name__ == '__main__':
    main(a);
